import { SignInRequestPayload } from './sign-in-request-payload';

describe('SignInRequestPayload', () => {
  it('should create an instance', () => {
    expect(new SignInRequestPayload()).toBeTruthy();
  });
});
