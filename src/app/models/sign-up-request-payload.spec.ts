import { SignUpRequestPayload } from './sign-up-request-payload';

describe('SignUpRequestPayload', () => {
  it('should create an instance', () => {
    expect(new SignUpRequestPayload()).toBeTruthy();
  });
});
